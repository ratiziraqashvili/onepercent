export type Product = {
    description: string;
    _id: string;
    sku: string;
    images: any;
    price: number;
    createdAt: null;
    name: string;
    currency: string;
    slug: string;
  }